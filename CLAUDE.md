# Project Rules

## Code Style

- Use explicit imports instead of `React.X` syntax (e.g., `import { useState } from 'react'` not `React.useState`)
- Export as part of declaration, not at the bottom of the file (e.g., `export function Foo() {}` not `export { Foo }`)
