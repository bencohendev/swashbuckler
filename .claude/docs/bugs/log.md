# Bug Log

Quick-fix bugs that don't need a full investigation doc. For complex bugs, create a separate file in this directory.

| Date | Bug | Feature | Fix |
|------|-----|---------|-----|
| 2026-02-20 | Stale entry visible after switching spaces — `switchSpace` only updates state/localStorage, doesn't navigate | Spaces | Redirect to `/` on space switch in SpaceSwitcher |
